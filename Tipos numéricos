package main

import "fmt"

var x = 10
var y = 10.5

func main() {
	a := "e"
	b := "é"
	c := "香"
	fmt.Printf("%v, %v, %v\n", a, b, c)
	d := []byte(a)
	e := []byte(b)
	f := []byte(c)
	fmt.Printf("%v, %v, %v\n\n\n", d, e, f)

	fmt.Printf("%v, %T\n", x, x)
	fmt.Printf("%v, %T", y, y)
}
